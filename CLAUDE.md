# Cloud tasks

For cloud work, read `docs/CLOUD-WORK.md` before setup or implementation, then the assigned issue. It defines the supported checks and the Mac/browser gates. Follow any applicable `AGENTS.md`.
